[random_numbers://<name>]
*Generates events containing a random floating point number.

min = <value>
max = <value>

# Linux Bash script
#[random_numbers://./bin/random_numbers.sh]
#*Generates events containing a random floating point number.
#
#min = <value>
#max = <value>

## Windows Batch script
#[random_numbers://.\bin\random_numbers.cmd]
#*Generates events containing a random floating point number.
#
#min = <value>
#max = <value>